# include<stdio.h>
int main()
{
    /*
int a =10,b=a;
printf("the value of a*b is %d",a*b);  (vailed instruction)

ques 4 
evaluation of 3*x/y-z+r where x=2 y=3 z=3 r=1

int x=2,y=10,z=3,r=1;

printf("evlaute 3*x/y-z+r : %d",3*x/y-z+r);

printf("multyply this 4*6-8*9 %d\n",4*6-8*9);

 write a program to check if a number is devide by 2 or not 

int X=2;
int a;

printf("enter a number :");
scanf("%d",&a);
printf("if output is 0 then its false and if output is 1 then true   : %d\n",a%X ==0);

int Y=2;
int B;

printf("enter a number :");
scanf("%d",&B);
printf("if output is 0 then its odd and if output is 1 then even   : %d\n",B%2 ==0);

int monday=0;
int raining=0;
in two any one then print 1
printf("%d\n",monday||raining);

int h;

printf("enter a number"); 
scanf("%d",&h);
printf("greater than 9 and less than 100 then true  : %d\n",(h>9) && (h<100));  */

/* write a program to print average 3 number*/
// int a1,a2,a3;
// float avg;
// avg = (a1+a2+a3)/3;           ---------- not solve-----------
// printf("enter the value ");

// scanf("%d %d %d\n",&a1 ,&a2 ,&a3);
// printf("the avg value is : %f\n",avg);




return 0;
}